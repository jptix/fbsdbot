install dependencies with ruby-gems:

	gem install ruby-irc	 ?remove this?
	gem install sqlite3-ruby
	gem install activerecord ?remove this?
	gem install treetop


edit bin/bot.conf.example, and save it to file bot.conf

put the bin-folder in path, or type: bin/fbsdbot.rb
